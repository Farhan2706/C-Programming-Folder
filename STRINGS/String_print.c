#include<stdio.h>
#include<string.h>  // For string 
int main(){
    
    char str[] = "Hello World";
    printf("%s\n",str);  // We can printf Without Loops.
    puts(str);  // for printing string.
    puts("Hello World"); // We can also print like printf. but only for strings.

    return 0;
}